<DefaultWindow xmlns="https://spacestation14.io"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         Title="{Loc 'document-printer-title'}"
                         MinSize="640 440">
    <BoxContainer Orientation="Horizontal" HorizontalExpand="True" VerticalExpand="True">
        <ScrollContainer MinWidth="160" VerticalExpand="True" HScrollEnabled="False">
            <BoxContainer Name="CategoryList"
                          Orientation="Vertical"
                          HorizontalExpand="True" />
        </ScrollContainer>

        <PanelContainer MinWidth="1" StyleClasses="LowDivider" />

        <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True" Margin="6 0 0 0">

            <PanelContainer StyleClasses="LowDivider" Margin="0 0 0 6">
                <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="6 4">
                    <Label Name="CopySlotLabel"
                           Text="{Loc 'document-printer-copy-empty'}"
                           HorizontalExpand="True"
                           VerticalAlignment="Center" />
                    <Button Name="EjectButton"
                            Text="{Loc 'document-printer-eject-button'}"
                            Margin="4 0 0 0" />
                    <Button Name="CopyButton"
                            Text="{Loc 'document-printer-copy-button'}"
                            Margin="4 0 0 0" />
                </BoxContainer>
            </PanelContainer>

            <LineEdit Name="SearchBar"
                      PlaceHolder="{Loc 'document-printer-search-placeholder'}"
                      Margin="0 0 0 6" />

            <ScrollContainer HorizontalExpand="True" VerticalExpand="True">
                <BoxContainer Name="DocumentList" Orientation="Vertical" />
            </ScrollContainer>
        </BoxContainer>

    </BoxContainer>
</DefaultWindow>
